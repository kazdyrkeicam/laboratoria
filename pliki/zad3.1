3. Napisz program w języku C, który zapisze do pliku 10 dowolnych liczb całkowitych. Do obsługi błędów użyj funkcji "perror".


#include <stdio.h>

int main(){
  FILE *out = NULL;
  
  if((out = fopen("test.bin", "wb")) == NULL){    //w czytanie b binarny tryb
    perror("blad");
    return 1;
  } 
  else {
    int arr[10] = {0};
    fwrite(arr, sizeof(int), 10, out);
    
    fclose(out);
  }
  return 0;
}

// no i powstal jakis plik test, sprawdzmy co w nim jest

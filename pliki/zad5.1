5. Napisz program w języku C, który usunie plik stworzony w poprzednim zadaniu.


#include <stdio.h>


int main(){

  if(remove("test") != 0){
    perror("Blad");
    return 1;
  }
  else
    printf("usunieto");
    
  return 0;
}

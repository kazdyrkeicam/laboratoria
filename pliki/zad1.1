#include <stdio.h>

/*
int find_min(int arr*, int n){
  int result = arr[0];
  for(int i = 0; i < n; ++i)
    if(result > arr[i])
      result = arr[i];
  return result;
}
/**/


void f(const char *filename){
  int arr[512], count = 0, min, max;
  FILE *in;
  
  if((in = fopen(filename, "rt")) == NULL)  printf("blad");
  else{
    while(feof(in) == 0){
      fscanf(in, "%d", &arr[count++]);
    } fclose(in);
  }
  
  printf("%d %d\n", arr[0], arr[count - 1]);
}

int main(){
  f("test");
  return 0;
}

#include <stdio.h>
#include <string.h>

  int main ()
  {
    char str1[] ="This is a simple string";
    char str2[10] = {0};

    scanf("%9s", str2);

    char *result  = strstr (str1, str2);
    printf("%s\n", result ? result : "nie znaleziono");


  return 0;
}

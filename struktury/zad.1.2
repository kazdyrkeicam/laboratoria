#include <stdio.h>
#include <math.h>

typedef struct{
  float x, y;
  
  /*  mod. do uzytkowania na pointerach
  float *x, *y;/**/
  
} Point;

int main(){
  Point a, b;
  //float a_value = 1, b_value = 2;
  
  /*a.x = a.y = &a_value;
  b.x = b.y = &b_value;
  /**/
  
  a.x = a.y = 1;
  b.x = b.y = 2;
  
  //scanf("%f%f%f%f", &a.x, &a.y, &b.x, &b.y);
  
  printf("%f\n", sqrt(pow(a.x - b.x, 2) + pow(a.y - b.y, 2)));
  return 0;
}
